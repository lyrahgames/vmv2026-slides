{
  "name": "vmv2026-slides",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "demo:build": "pnpm --dir demo run build",
    "build:all": "pnpm run demo:build && pnpm run build",
    "dev": "pnpm run demo:build && slidev --open",
    "export": "slidev export"
  },
  "dependencies": {
    "@slidev/cli": "^52.19.1",
    "@slidev/theme-default": "latest",
    "vue": "^3.5.33"
  }
}
